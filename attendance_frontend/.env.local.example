# Public base URL for the backend API
# In local dev, backend runs on http://localhost:3001
REACT_APP_API_BASE=http://localhost:3001

# Optional: run CRA dev server on 3000 (default) or 3002 if you prefer
# PORT=3000
