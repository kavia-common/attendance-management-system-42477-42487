# Local environment example for Attendance Frontend

# Backend API base URL
REACT_APP_API_BASE=http://localhost:3001

# Frontend dev server port (optional)
PORT=3002

# Disable CRA Fast Refresh completely to avoid $RefreshSig runtime helpers
FAST_REFRESH=false
REACT_APP_DISABLE_FAST_REFRESH=true

# Optional: disable source maps if troubleshooting sourcemap consumer issues
# REACT_APP_ENABLE_SOURCE_MAPS=false
